- All necessary do-files should exist in the directory where redo runs.
- If redo is invoked to build the target which already exists but not
  generated by redo, redo just uses the existing target as an result
  and any applicable do-files are ignored. Redo
- Once the target is generated by redo, it requires at least one
  applicable do-file for it. If none exists, redo fails with error.
